public interface LinkedListInterface {
    //target 앞에 node 삽입, a - target - b => a - new node - target - b
    void insert(Node target, int number);
    LinkedList add(LinkedList smallNumber);
}
